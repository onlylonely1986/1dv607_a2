namespace view
{
    public enum Event {
        None,
        NewMember,
        SearchMemberName,
        SearchMemberId,
        ChangeMember,
        ChangeBoat,
        AddBoat,
        RemoveBoat,
        RemoveMember,
        CompactList,
        VerboseList,
        SearchWordGiven,
        GoBack,
        Quit
    }
}